# أكاديمية الإمام — Release 9.2.2

هذا الإصدار مبني على المشروع الكامل v9.2.1، وليس الحزمة المختصرة السابقة. تم الحفاظ على ملفات الهوية الأصلية، الصور المرجعية، SQL، الاختبارات والتوثيق.

## أهم إصلاحات الاستقرار

- تعارض المواعيد يخص الطلاب النشطين فقط.
- إعادة تنشيط الطالب من القائمة الفردية أو التحديد المتعدد.
- تقوية PIN المحلي باستخدام PBKDF2-SHA256 مع Salt و250,000 دورة، مع ترقية تلقائية من سجل SHA-256 القديم بعد أول تحقق صحيح.
- قفل مؤقت بعد خمس محاولات PIN خاطئة.
- حذف الطالب ينظف التسجيلات الصوتية المحلية التابعة لحصصه.
- Data Health يفحص الملاحظات الصوتية المفقودة/اليتيمة واستخدام التخزين.
- عند تعذر تحديد رقم صفحة المصحف لا يتم فتح الصفحة 1 كبديل خاطئ.
- إزالة تحديث Mini Player مع كل حدث `timeupdate` لتقليل عمل DOM غير الضروري.
- الصفحة الرئيسية تستهدف الحصة التالية أو المتأخرة مباشرة عندما يوجد جدول.
- تحسين `aria-current` لمسار خطوات الحصة وتحسين تباين النص الثانوي.
- Service Worker يستخدم Precache متسامحًا مع فشل أصل واحد وحدًا لكاش Runtime.

## التوافق

- Schema Version ما زال **11**.
- لا توجد Migration مدمرة لبيانات الطلاب أو الحصص أو الإعدادات.
- PIN القديم يظل قابلًا للتحقق ثم يتم ترقيته تلقائيًا إلى الصيغة الأقوى.
- بيانات v9.2.1 الحالية تبقى متوافقة.

## قبل النشر

شغّل:

```bash
npm test
```

ثم ارفع **محتويات المجلد بالكامل** إلى جذر مستودع GitHub. إذا كنت تستخدم GitHub Pages من الفرع `main` فاختر `/ (root)`.
