# نشر أكاديمية الإمام v8.0.0

## قبل أي ترقية

1. افتح النسخة الحالية المستخدمة فعلياً.
2. صدّر **Backup JSON**.
3. احتفظ بالنسخة في مكان خارج المتصفح.
4. لا تمسح بيانات الموقع قبل التأكد أن v8 قرأ البيانات القديمة بصورة صحيحة.

Schema الحالي: **10**.

## الملفات التي يجب أن تكون في جذر المستودع

```text
index.html
styles.css
app.js
v8.js
sw.js
manifest.json
favicon.png
icon-96.png
icon-192.png
icon-512.png
icon-maskable.png
CNAME
```

مع المجلدات:

```text
screenshots/
branding/
sql/
tests/
```

## الفحص قبل الرفع

```bash
npm test
```

ثم شغّل نسخة محلية:

```bash
python3 -m http.server 8080
```

وافتح:

```text
http://localhost:8080
```

## GitHub Pages

داخل GitHub:

**Settings → Pages → Build and deployment → Deploy from a branch**

ثم:

- Branch: `main`
- Folder: `/(root)`

ملف `CNAME` يحتوي:

```text
welivequran.online
```

بعد إعداد DNS فعّل **Enforce HTTPS**. HTTPS مهم للتثبيت كـPWA وميزات WebAuthn والبصمة/Windows Hello.

## الترقية من v7.1

ارفع ملفات v8 فوق الملفات الموجودة مع الاستبدال، ولا ترفع ZIP نفسه.

أهم الملفات الجديدة/المتغيرة:

- `v8.js` — طبقة الميزات الجديدة.
- `app.js` — Version 8.0.0 وSchema 10 + Bridge للطبقة الجديدة.
- `index.html` — عناصر UI الجديدة.
- `styles.css` — تصميم v8.
- `sw.js` — Cache جديد ويشمل `v8.js`.
- `README.md` / `CHANGELOG.md`.

بعد النشر قد يظهر Banner **يوجد إصدار جديد**. اضغط تحديث الآن. إذا ظل جهاز على نسخة قديمة بعد النشر، أغلق التطبيق وافتحه مرة أخرى ثم نفّذ تحديث Service Worker.

## Checklist بعد النشر

- الرئيسية تعرض الإحصائيات والتحليل والأهداف الأسبوعية في الأعلى.
- لا توجد بطاقتا «الحصة التالية» أو «يحتاج متابعة» في الرئيسية.
- التقييم يظهر فقط داخل «تسميع الحصة السابقة».
- الحفظ والمراجعة الحالية لا تعرض نسباً مئوية.
- الحفظ الجديد يحتوي زر **📖 النص**.
- اختيار السورة يعمل بالكتابة والقائمة المنسدلة.
- أرقام الآيات لا تتجاوز عدد آيات السورة.
- الاستكمال بعد `المنافقون 1–3` يبدأ من 3 أو 4 حسب إعداد الطالب.
- عند نهاية `المجادلة`: الاتجاه ↑ يقترح `الحديد` والاتجاه ↓ يقترح `الحشر`.
- مراجعة الأجزاء ومراجعة السور مستقلتان.
- الأجزاء تظهر بأسمائها: «جزء تبارك»، «جزء عمّ»... إلخ.
- سجل الأخطاء يعمل ويظهر في ملف الطالب.
- Timeline وخريطة المصحف يظهران في ملف الطالب.
- تسجيل الملاحظة الصوتية يعمل عند منح إذن الميكروفون.
- رسالة واتساب المختصرة والتفصيلية تعملان.
- تقرير PDF يعمل من نافذة الطباعة.
- PIN يعمل، وWebAuthn يُختبر على HTTPS فقط.
- Backup التلقائي يظهر في الإعدادات.
- PWA تثبت على Android وWindows.
- أوقف الشبكة بعد أول تحميل وتأكد أن Shell الأساسي يعمل Offline.

## تفعيل المزامنة بين Android وWindows — اختياري

1. أنشئ مشروع Supabase.
2. افتح **SQL Editor**.
3. نفّذ الملف:

```text
sql/supabase-sync.sql
```

4. في التطبيق افتح **الإعدادات → مزامنة Android وWindows**.
5. أدخل:
   - Project URL.
   - anon/publishable key.
   - Sync ID صعب التخمين.
   - كلمة تشفير قوية لا تقل عن 8 أحرف.
6. استخدم **رفع النسخة المشفرة** من الجهاز الأساسي.
7. على الجهاز الثاني أدخل نفس الإعدادات ثم استخدم **تنزيل ودمج**.
8. بعد التأكد يمكن تفعيل المزامنة التلقائية.

### ملاحظات أمنية للمزامنة

- جدول Supabase يرى payload مشفراً، وليس بيانات الطلاب نصاً صريحاً.
- لا تُرفع كلمة التشفير ولا PIN ولا Credential الخاص بالبصمة.
- سياسة RLS المرفقة تسمح للـanon بالوصول إلى صفوف الجدول، لذلك الحماية الفعلية للمحتوى تعتمد على **AES‑GCM + كلمة تشفير قوية + Sync ID غير سهل التخمين**.
- لا تضف DELETE policy ما لم تكن تحتاج الحذف السحابي فعلياً.
- الملاحظات الصوتية لا تدخل المزامنة في v8.0.

## مصادر النص القرآني والصوت

- العرض القرآني يستخدم QPC Hafs / الرسم العثماني، مع خط Uthmanic Hafs من Quran Foundation CDN.
- التطبيق يستخدم endpoint نصي من Quran.com في الواجهة الثابتة، مع fallback للرسم العثماني.
- الصوت للحصري والعفاسي يستخدم ملفات EveryAyah بنمط آية منفصلة.

يحتاج ذلك اتصال إنترنت. لا تضع مفاتيح سرية أو `client_secret` داخل JavaScript المنشور على GitHub Pages.

## تغيير الإصدار مستقبلاً

حدّث معاً:

1. `APP_VERSION` في `app.js`.
2. application-version في `index.html`.
3. `CACHE_NAME` و`APP_VERSION` في `sw.js`.
4. `VERSION`.
5. `package.json`.
6. `CHANGELOG.md`.

وإذا تغير نموذج البيانات ارفع `SCHEMA_VERSION` وأضف Migration/Sanitization مناسباً.
